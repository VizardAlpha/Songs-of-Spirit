package vizardalpha.songsofspirit.quest;

import java.util.Observable;
import java.util.Observer;

public class QuestRequirementObserver implements Observer {
    @Override
    public void update(Observable o, Object arg) {
        
    }
}
