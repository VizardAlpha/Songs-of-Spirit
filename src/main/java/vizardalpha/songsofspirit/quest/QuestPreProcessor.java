package vizardalpha.songsofspirit.quest;

import vizardalpha.songsofspirit.quest.data.QuestEntry;

public class QuestPreProcessor {
    public QuestEntry process(QuestEntry entry) {

        // TODO: increase requirements and reward with citizen amount
        return entry;
    }
}
